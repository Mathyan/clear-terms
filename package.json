{
  "name": "clear_terms",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd frontend && npx ng serve\" \"cd backend && npm run start:debug\"",
    "install": "cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "concurrently": "^4.1.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/cookie-parser": "^1.4.7"
  }
}
